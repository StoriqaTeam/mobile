import { createIconSetFromFontello } from 'react-native-vector-icons';
import fontelloConfig from '../../fonts/config.json';


const StoriqaIcon = createIconSetFromFontello(fontelloConfig);

export default StoriqaIcon;
